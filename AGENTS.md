## Agent skills

### Issue tracker

Use GitHub Issues in `42lizard/pocketbook`. Before tracker operations,
read `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels. Before triaging,
read `docs/agents/triage-labels.md`.

### Domain docs

Use separate contexts for each app. Before exploring or changing code,
read `docs/agents/domain.md`.
